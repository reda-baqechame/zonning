export type FeedFomoMeta = {
  plan: string;
  poolPermits: number;
  poolHighValue: number;
  poolUrgentTenders: number;
  shownPermits: number;
  shownTenders: number;
  hiddenHighValue: number;
  hiddenUrgent: number;
  estimatedValueHidden: number;
};

export default function MissedOpportunityBanner() {
  return null;
}
